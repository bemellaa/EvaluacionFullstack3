version: '3.8'

services:
  postgres-db:
    image: postgres:15-alpine
    container_name: smartlogix-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
      POSTGRES_DB: smartlogix_db
    ports:
      - "5432:5432"
    restart: always