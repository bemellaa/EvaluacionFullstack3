server:
  port: 8089

spring:
  application:
    name: ms-pagos
  datasource:
    url: jdbc:postgresql://localhost:5432/TU_BASE_DE_DATOS # Cambia esto si tu BD se llama distinto
    username: postgres
    password: GkCMQaTgcoo4ETva # Confirma si esta es tu clave
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect